Proceed with v110 — Domain Verification Intake + Static Design Dashboard/Report Skeleton.

Use v109 as the base. Do not alter upstream triage/research/setup/contact-ready routing.

Goal:
Accept domain verification results, mark packages with 10 verified domains as package_ready, and create the skeleton for later customer-facing dashboard/report materialization without fully generating public pages yet.

Touch:
- sales-preview-package-workbench.php
- preview-package-model.php
- assets/css/admin.css
- optionally add design-dashboard-model.php if cleaner

Required:
1. Add domain verification intake.
   - Accept JSON shaped like package_batch.batch_type = domain_availability_verification and domain_results[].
   - Store:
     verified_domain_options_json
     package_status
     package_warnings_json
     package_next_step
   - If verified_domain_options count >= 10, set package_status=package_ready.
   - If fewer than 10, set manual_package_review or domain_check_needed based on result.

2. Workbench routing:
   - Package Ready pile should display packages that passed domain verification.
   - Active prompt should no longer be domain check when none are waiting.

3. Add static materialization skeleton:
   - Define intended paths:
     /go/{short_slug}
     /design/{short_slug}
     /report/{short_slug}
   - Do not build full public pages yet.
   - Add collapsed preview of what would be generated.

4. Add package validation helper:
   - package_ready requires:
     short_slug
     hotlink_path
     design_dashboard_path
     sales_report_path
     10 web design options
     10 logo options
     10 verified domains
     sales report
     no blocking warnings

5. UX:
   - iPhone-first.
   - One active prompt/action.
   - Domain verification intake collapsed.
   - Clear Package Ready count.
   - Clear Ready For Marketing remains future state.

Do not:
- no outreach/send
- no SMS
- no AI calls
- no domain purchase
- no live domain API
- no full customer-facing page generation yet
- no upstream workflow changes
