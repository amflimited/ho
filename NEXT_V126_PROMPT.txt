Proceed with v126 — Front Door Preview Renderer.

Use v125 as the base. Do not change diagnosis import logic. Do not change dashboard next-move logic except to link to the new renderer/builder if needed.

Goal:
Build the actual customer-facing pre-sale Front Door Preview page model:
- /go.php?slug={business_slug}
This is the sales asset. It replaces the old idea of a giant pre-sale design dashboard.

Touch:
- front-door-preview-model.php
- go.php
- assets/css/front-door.css or assets/css/admin.css only if simpler
- sales-portal-dashboard.php only if a link label needs adjustment
- command-center-model.php only if state detection needs to recognize the new go_path claim later
- diagnosis-model.php only if rendering helper is clearly needed

Required:
1. Customer-facing route:
   - /go.php?slug={business_slug}
   Start with query-string routing. Do not attempt /go/{slug} rewrite yet.

2. The page must render from stored records and diagnosis claims:
   - business_name_current
   - business_slug
   - business_type
   - location_city/state
   - strength_keys_json
   - weakness_keys_json
   - recommendation_keys_json
   - primary_offer_path
   - preview_direction_keys_json

3. Page sections:
   - personalized intro
   - what we noticed
   - what is already helping you
   - what may be costing you customers
   - what we would fix first
   - exactly 3 preview directions
   - simple offer
   - CTA

4. Use block templates from diagnosis-model.php / diagnosis registries.
   - Do not generate custom copy.
   - Do not require GPT.
   - Do not require package_status.
   - Do not require old materialization.

5. Preview directions:
   - Show exactly 3 direction cards when possible.
   - Each card shows:
     direction label
     short description
     personalized business name
     sample CTA

6. Offer:
   - Use primary_offer_path.
   - standard_front_door fallback.
   - CTA:
     Start My Front Door
     Ask Adam A Question
   - Use adam@hoosieronline.com.

7. Missing data handling:
   - If slug missing or not found, show friendly not-found page.
   - If diagnosis keys missing, show “preview not ready yet” page.
   - Never blank page.

8. Runtime safety:
   - function_exists/file_exists guards where needed.
   - Try/catch around loading record.
   - No fatal if diagnosis-model.php missing; show error card.

9. Admin support:
   - No full builder yet.
   - Add a small read-only test link pattern in README:
     /go.php?slug={business_slug}

Acceptance test:
For a diagnosis-ready business, opening /go.php?slug={business_slug} must show:
1. business-specific intro
2. at least one strength
3. at least one weakness
4. at least one recommendation
5. three preview direction cards if 3 keys exist
6. offer
7. CTA

Do not:
- no sending
- no SMS
- no AI calls
- no payment
- no outreach automation
- no domain purchasing
- no static file generation yet
- no 10-template dashboard
- no post-sale intake
- no changes to diagnosis import logic
