Hoosier Online v052 Final Hardening

Future upload package contract locked for this project:
1. Every package must include deploy-manifest.json at ZIP root.
2. Every package must place site files at ZIP root paths matching their install locations.
3. Do not include root junk files.
4. Do not include database credentials, admin secrets, logs, backups, SQL dumps, or generated ZIPs.
5. Prefer narrow packages that touch only the files required for the task.
6. Include a short package purpose/version in deploy-manifest.json.
7. Run PHP syntax checks before packaging.
8. Keep admin/system pages behind admin-core.php / admin-auth.php.
9. Avoid customer-facing UI, scraping, outreach automation, or payment work unless explicitly approved.
10. For deployment packages, assume upload.php requires deploy-manifest.json.

Post-install hardening steps:
1. Login at /admin-login.php.
2. Open /admin-hardening.php.
3. Create private directory outside public_html:
   /home1/spofnkte/hoosier-online-private
4. Copy private/admin-secrets.example.php to:
   /home1/spofnkte/hoosier-online-private/admin-secrets.php
5. Generate a new password hash and put it in the private admin-secrets.php.
6. Copy private/database.local.example.php to:
   /home1/spofnkte/hoosier-online-private/database.local.php
7. Fill in live DB credentials.
8. Confirm /sales-db-check.php works while logged in.
9. Remove or neuter public admin-secrets.php after private secrets are confirmed.
10. Keep database.php as loader/fallback only.
11. Do not test sales workflow until hardening status is acceptable.

Manual test remains blocked until:
- login works
- upload is protected
- backup is protected
- DB check is protected
- private secrets are configured or public fallback is intentionally accepted temporarily
