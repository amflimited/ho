v068 Public Homepage Porch Overlays

Scope:
- index.php only.

Changes:
- Moves the hero "The porch light's on" note card out of the inline page and into a closeable overlay.
- The intro overlay fades in one second after page load.
- Removes the inline bottom "We'll leave the porch light on" CTA section.
- Adds a bottom-page trigger that opens the CTA as a closeable overlay when the visitor reaches the end of the page.
- Updates Say hello / pricing CTAs to mailto links because the #porch inline section no longer exists.
- Preserves all current public copy.

No admin changes.
No database changes.
No preview.php.
No scraping/payment/outreach changes.
