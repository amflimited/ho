name: Deploy to HostGator

on:
  push:
    branches: ["claude/admin-site-assessment-4Zrig"]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Deploy via SFTP
        env:
          SSHPASS: ${{ secrets.FTP_PASSWORD }}
        run: |
          sudo apt-get install -y lftp sshpass
          lftp -u "${{ secrets.FTP_USERNAME }},dummy" \
            -e "set sftp:connect-program 'sshpass -e ssh -o StrictHostKeyChecking=no'; \
                mirror --reverse --verbose \
                  --exclude-glob .git/ \
                  --exclude-glob .github/ \
                  --exclude database.php \
                  --exclude admin-secrets.php \
                  --exclude stripe-config.php \
                  --exclude porkbun-config.php \
                  . /public_html/; \
                bye" \
            sftp://${{ secrets.FTP_HOST }}
