Proceed with v109 — Preview Package Intake + Domain Candidate Check Prompt.

Use v108 as the base. Do not alter the upstream triage/research/setup/contact-ready pipeline.

Goal:
Allow the Preview Package Workbench to accept the package-generation JSON and stage packages for domain verification, then generate a copy/paste GPT prompt to check domain availability.

Touch:
- sales-preview-package-workbench.php
- preview-package-model.php
- assets/css/admin.css
- sales-portal-dashboard.php only if a navigation link needs adjustment

Required:
1. Add package result intake to /sales-preview-package-workbench.php.
   - Accept JSON shaped like package_batch + packages[] from v108 prompt.
   - Do not send outreach.
   - Do not generate public pages yet.
   - Do not purchase domains.

2. Store package results using existing claim/evidence import style if possible.
   - No schema changes unless absolutely unavoidable.
   - At minimum store:
     package_status
     short_slug
     hotlink_path
     design_dashboard_path
     sales_report_path
     recommended_template_key
     web_design_options_json
     logo_options_json
     domain_candidates_json
     sales_report_json
     package_warnings_json
     package_next_step

3. Route package statuses:
   - domain_check_needed
   - manual_package_review
   - package_blocked
   - package_drafted if incomplete

4. Add active prompt for Domain Availability Check when domain_check_needed records exist.
   - Prompt should include the 20 domain candidates for each package.
   - It should ask GPT/domain-check workflow to return exactly 10 verified available domains per business where possible.
   - If fewer than 10 are available, return status domain_check_failed or manual_package_review.

5. Domain availability rule:
   - Do not mark package_ready until verified_domain_options has 10 available domains.
   - Domain candidates are not enough.

6. UX:
   - iPhone-first.
   - One active prompt at a time.
   - Package intake area collapsed unless needed.
   - Clear status counts.
   - Clear “Return to Work Queue” button.

Do not:
- no customer-facing dashboard/report generation yet
- no outreach/send
- no SMS
- no AI calls
- no domain purchasing
- no live API unless already available and simple
- no changes to lead/research/setup routing
