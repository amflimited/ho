Proceed with v141 — Production Lane Test + Reality Check.

Use v140 as the base.

Goal:
Do not add major features. Add a lightweight read-only test/check page that verifies the production lane is selecting the expected current job and explains why.

New page:
- /sales-production-lane-check.php

Touch:
- sales-production-lane-check.php
- production-lane-model.php only if small helper needed
- sales-portal-dashboard.php link only if needed
- assets/css/admin.css

Required:
1. Read-only only.
2. Show:
   - current selected job
   - priority order
   - counts used:
     send_ready
     prep_ready
     intake_waiting
     problem_records
     known_records
   - records sampled for the selected job where safe
   - why lower-priority jobs were skipped
3. Must help diagnose if the app appears to pick the wrong job.
4. No operational writes.
5. No sending, SMS, AI calls, scraping automation, payments, or domain purchasing.

Acceptance test:
Adam can open /sales-production-lane-check.php and see exactly why the home page chose its current job.
