Proceed with v125 — Command Center Dashboard Replace.

Use v124 as the base. Use the state logic proven in /sales-command-center-audit.php. Do not change diagnosis import logic. Do not mutate records in this pass except normal page rendering.

Goal:
Replace the top of the main admin dashboard with a real next-action command center driven by the v124 audit state model.

Touch:
- sales-portal-dashboard.php
- command-center-model.php only if minor helper adjustments are needed
- assets/css/admin.css

Required:
1. The main dashboard must start with one clear Next Move card.
   It must show:
   - next_move_key
   - title
   - why this is next
   - count affected
   - Do This Now button
   - expected result after completion

2. The dashboard must answer immediately:
   - What do I do next?
   - Why?
   - How many records?
   - Where do I click?
   - What happens after?

3. Use v124 next-move priority:
   1. Review Data Problems if severe identity/conflict problems exist
   2. Run Diagnosis Batch if Contact Ready without diagnosis exists
   3. Build /go Front Door Preview Pages if diagnosis-ready records lack /go pages
   4. Draft Outreach if /go pages exist but outreach drafts do not
   5. Manual Send Review if drafts are ready
   6. Find New Leads if no active production work exists

4. Add a secondary “After This” card.
   It should explain the likely next step after the current one.

5. Replace or demote vague downstream cards.
   The main dashboard should not lead with:
   - Preview Package Workbench
   - Package System
   - old materialization/domain check language

6. Add a Sales Asset Queue section near the top:
   - Contact Ready without diagnosis
   - Diagnosis Ready without /go preview
   - /go Page Ready
   - Outreach Draft Needed
   - Draft Ready
   - Manual Review Needed

7. Move old pile cards lower and collapse them under:
   Pipeline Details

8. Move data-problem buckets lower and collapse them under:
   Data Problems

9. Keep a button to:
   /sales-command-center-audit.php
   labeled “Open Full State Audit”

10. iPhone-first:
   - no giant raw blocks
   - no fake counts
   - no vague labels
   - main action above the fold
   - old piles below the fold

11. Runtime safety:
   - If command-center-model.php is missing or errors, show an error card and do not blank page.
   - Use ho_salesportal_list_businesses_with_readiness(null, '') with the real signature.
   - Do not assume array argument signature.

Acceptance test:
After installing v125, opening /sales-portal-dashboard.php must show:
1. A single obvious Next Move at the top
2. A Do This Now button
3. A count affected
4. Why it is next
5. What happens after
6. Old lead piles no longer dominate the first screen
7. Link to full /sales-command-center-audit.php

Do not:
- no sending
- no SMS
- no AI calls
- no payment
- no outreach automation
- no domain purchasing
- no /go renderer yet
- no diagnosis import changes
- no old Preview Package Workbench changes except demotion/legacy labeling if needed
