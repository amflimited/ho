Proceed with v127 — Front Door Builder / Preview State Writer.

Use v126 as the base. Do not change diagnosis import logic. Do not change the /go renderer except if a small helper is needed.

Goal:
Create the admin bridge that takes diagnosis-ready businesses and marks them as having a usable /go preview path.

New page:
- /sales-front-door-builder.php

Touch:
- sales-front-door-builder.php
- front-door-preview-model.php only if helper needed
- command-center-model.php if state detection needs adjustment
- prospect-model.php only if canonical claim fields are needed
- sales-portal-dashboard.php only if button target/label needs adjustment
- assets/css/admin.css

Required:
1. Batch-first admin page with piles:
   - Front Door Preview Needed
   - Front Door Preview Ready
   - Manual Review

2. Load businesses that are diagnosis-ready but do not have go_path/go_slug.

3. Primary action:
   Create /go Preview Batch

4. This should not require GPT.
   The /go page renders dynamically from diagnosis keys.
   The builder only needs to assign/store:
   - front_door_preview_status = go_ready
   - go_slug
   - go_path
   - go_preview_version
   - outreach_asset_url

5. Slug rule:
   - use existing business_slug as default go_slug
   - go_path = /go.php?slug={business_slug}
   - do not attempt rewrite routing yet

6. Add canonical claim fields if needed:
   - go_slug
   - go_path
   - go_preview_version
   - outreach_asset_url

7. Intake/write safety:
   - Use the real import function adapter pattern from diagnosis workbench.
   - Do not update rows directly if existing claim import system can be used.
   - Batch write claims only for selected diagnosis-ready records.
   - Show result count.

8. After successful creation:
   - Diagnosis Ready without /go should decrease
   - /go Page Ready should increase
   - dashboard next move should be able to advance toward Draft Outreach

9. UI:
   - iPhone-first
   - one main action
   - show generated path examples
   - collapsed record list
   - link to test /go.php?slug={business_slug}

Acceptance test:
After installing v127:
1. Open /sales-front-door-builder.php
2. See diagnosis-ready records needing /go path
3. Click Create /go Preview Batch
4. Records get go_slug/go_path claims
5. /sales-command-center-audit.php shows /go Page Ready increased
6. /go.php?slug={business_slug} opens the customer-facing preview

Do not:
- no sending
- no SMS
- no AI calls
- no payment
- no outreach automation
- no domain purchasing
- no static file generation
- no 10-template dashboard
- no post-sale intake
- no diagnosis import changes
