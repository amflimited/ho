Proceed with v134 — Intake Module: Candidate Lead Preview.

Use v133, v132, and v131a as the source of truth.

Goal:
Build the Intake module that accepts Source candidate JSON and previews how it would convert into business records before any durable import.

New page:
- /sales-intake.php

Touch:
- sales-intake.php
- intake-model.php if cleaner
- source-model.php only if shared helpers are needed
- sales-portal-dashboard.php link only
- assets/css/admin.css

Required:
1. iPhone-first app-like Intake page.
2. Accept pasted JSON shaped:
   candidate_batch.batch_type = source_candidates
   candidates[]
3. Do not write durable business records yet unless explicitly safe and separately confirmed.
4. Build Intake Preview groups:
   - new_business
   - update_existing
   - possible_duplicate
   - needs_review
   - reject
5. Map candidate fields into proposed business fields:
   - business_name_current
   - business_slug
   - business_type
   - location_city
   - location_state
   - website_url
   - facebook_url
   - google_profile_url
   - email_address
   - phone_number
   - source_context
6. Dedupe detection:
   Exact duplicate:
   - same normalized website
   - same Facebook URL
   - same Google profile URL
   - same email
   - same phone
   - same existing slug
   Likely duplicate:
   - same normalized name + same city/state
   - similar name + same phone
   - same business name + nearby city
   Not duplicate by itself:
   - same category
   - same city
   - similar generic words only
7. Show preview decision reasons.
8. Show counts by group.
9. No durable import in v134 unless the page only stages a preview in memory/session.
10. No sending, SMS, AI calls, scraping automation, payments, or domain purchasing.

Acceptance test:
Adam can paste Source JSON and see which candidates are new, updates, possible duplicates, needs review, or rejected before any import.
